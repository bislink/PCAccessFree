% layout 'default';
% title 'Admin Panel - ' . session->{user} . ' - ';

% if ( session->{user} ) {
  <article class="panel dashboard"><h2>Admin Panel</h2>
    <a href="<%= url_for '/admin/settings' %>" title="setings">Settings</a>
    <a href="<%= url_for '/logout' %>">Logout</a>
  </article>
% } else {
  <article class="footer"><h2>Welcome back</h2>
    <%= session->{user} %>
  </article>
% }
